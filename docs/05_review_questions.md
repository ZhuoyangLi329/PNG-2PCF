# Review questions for GPT

1. Reconstruct the 9.18 baseline xi operator and verify shell averaging, mother-box support, `sigma_s`, GIC and BAO-mask contracts.
2. Reconstruct the 9.22 GSM PNG response and identify missing CLPT-PNG operators, especially velocity--velocity and higher-bias terms.
3. Test whether the center-valued hybrid xi and shell-averaged 9.18 xi are being compared under the same radial estimator definition.
4. Audit full RIC: midpoint/endpoint LOS, radial projector normalization, RR division, shot terms, `sn0` projection, and one-time GIC accounting.
5. Rebuild P/xi/joint likelihoods from the frozen 74-vector and verify all covariance block ordering and Hartlap/Percival use.
6. Quantify whether the joint b1 offset is generated by mean-model shape mismatch, cross covariance, or a covariance backend mismatch.
7. Propose the minimum falsifiable next experiment, with expected signatures under each hypothesis.
8. Do not interpret numerical convergence, small raw chi2, or overlapping marginal intervals as model closure by themselves.
