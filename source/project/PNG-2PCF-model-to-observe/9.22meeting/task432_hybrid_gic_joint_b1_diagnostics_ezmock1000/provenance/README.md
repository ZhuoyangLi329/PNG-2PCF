Initial numerical runs included two covariances, before the user requested EZmock1000-only analysis. These three source snapshots reproduce those already-completed calculations. The current code/ runner defaults to EZmock1000 only. Final PDF, summary, CSV and full diagnostic archive present EZmock1000 only.

The initial operator artifact included an available-window mean from ph000 only; this was removed during audit. The final operator artifact has no p_window_mean array and explicitly lists the 24 missing phase windows. No 25-window result is claimed.
